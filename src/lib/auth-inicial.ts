/**
 * Contraseña inicial del usuario `admin` tras `npm run prisma:seed`
 * o bootstrap en desarrollo (tabla usuarios vacía).
 * Debe coincidir con `prisma/seed.js`.
 */
export const CLAVE_ADMIN_INICIAL = "Admin1234.v1";
